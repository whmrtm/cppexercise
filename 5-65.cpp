
#include <iostream>
#include <cstdlib>
#include <vector>
#include <string>

using namespace std;
bool isSameNode(node* root1, node* root2){
  if (root1 != NULL && root2 != NULL)
		return true;
}

int main(){
  if (root1 != NULL && root2 != NULL)
	{
     return (isSameNode(root1->left) && isSameNode(root2 ->left))
	}
	return 0;
}  
